services:
  - type: worker    # ✅ 'web' ki jagah 'worker' use karo
    name: mantri-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9